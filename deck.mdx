import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github, vsDark } from "@code-surfer/themes";

export const theme = vsDark;

# Hello world

---

<CodeSurfer>

```typescript
interface RendererInterface {
  drawCircle(x: number, y: number, radius: number, color: number): void
}
```

```typescript
interface RendererInterface {
  drawCircle(x: number, y: number, radius: number, color: number): void
}

class Circle {
  constructor(public x: number, public y: number, public radius: number, private renderer: RendererInterface) {}
}
```

```typescript
interface RendererInterface {
  drawCircle(x: number, y: number, radius: number, color: number): void
}

class Circle {
  constructor(public x: number, public y: number, public radius: number, private renderer: RendererInterface) {}

  draw(color: number) {
    this.renderer.drawCircle(this.x, this.y, this.radius, color)
  }
}
```

```typescript
interface RendererInterface {
  drawCircle(x: number, y: number, radius: number, color: number): void
}

class Circle {
  constructor(public x: number, public y: number, public radius: number, private renderer: RendererInterface) {}

  draw(color: number) {
    this.renderer.drawCircle(this.x, this.y, this.radius, color)
  }
}

class CanvasRenderer implements RendererInterface { /* ... */ }
```

```typescript 13:17
interface RendererInterface {
  drawCircle(x: number, y: number, radius: number, color: number): void
}

class Circle {
  constructor(public x: number, public y: number, public radius: number, private renderer: RendererInterface) {}

  draw(color: number) {
    this.renderer.drawCircle(this.x, this.y, this.radius, color)
  }
}

class CanvasRenderer implements RendererInterface { /* ... */ }

const circle = new Circle(0, 0, 32, new CanvasRenderer())
circle.draw(0x0000ff)
```

```typescript 14:17
interface RendererInterface {
  drawCircle(x: number, y: number, radius: number, color: number): void
}


class Circle {
  constructor(public x: number, public y: number, public radius: number, private renderer: RendererInterface) {}

  draw(color: number) {
    this.renderer.drawCircle(this.x, this.y, this.radius, color)
  }
}

class WebGLRenderer implements RendererInterface { /* ... */ }

const circle = new Circle(0, 0, 32, new WebGLRenderer())
circle.draw(0x0000ff)
```

</CodeSurfer>

---

<CodeSurferColumns>

<Step subtitle="TypeScript to JavaScript">

```ts
const PI: number = 3.14
```

```js
"use strict";
const PI = 3.14;
```

</Step>
</CodeSurferColumns>
